dist: xenial
language: java

notifications:
  email: false

jdk:
  - openjdk10
  - openjdk11

cache:
  directories:
    - $HOME/.m2

services:
  - xvfb

script:
  - mvn test -B
  - mvn clean package -DskipTests=true
  - ./smoketest/smoke_test.sh
